# Make the engines field in package.json enforced, not advisory. Without this,
# a Node version outside engines only warns -- including in CI, where
# setup-node falls back to the runner's preinstalled Node if it cannot read
# engines.node. That fallback is silent and looks exactly like success.
engine-strict=true
